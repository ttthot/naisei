<h1>投稿一覧</h1>
<ul>
    <% @posts.each do |post| %>
        <li>
        <!-- titleフィールドがないのでトピックのタイトルを表示 -->
        <h2><%= post.topic.title %></h2>
        <p><%= post.content %></p>
        <%= post.created_at %>
        </li>
    <% end %>
</ul>