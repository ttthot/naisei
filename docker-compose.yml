services:
  db:
    image: mysql:8.4
    volumes:
      - mysql_data:/var/lib/mysql

  web:
    build:
      context: .
    depends_on:
      - db

  nginx:
    image: nginx:stable-alpine
    depends_on:
      - web

volumes:
  mysql_data:
